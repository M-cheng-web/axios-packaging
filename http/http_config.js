/**
 * 负责控制全局的一些配置
 * 这些配置只是默认配置,业务接口可覆盖
 * todo: 提供后端给的数据结构，比如正确码，data名这俩个，省的每次都要手动改这俩个东西
 */

export const DATA_KEY = 'appdata' // 请求返回内容名

export const CODE_KEY = 'code' // 请求返回的code名

export const SUCCESS_CODE = '0000' // 成功code
